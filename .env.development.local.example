# Development-specific environment variables
# Copy this to .env.development.local (gitignored) for local development overrides

# Enable DevTools automatically on app start
FORCE_DEV_TOOLS=true

# Local Ollama configuration (adjust if using non-default port)
OLLAMA_BASE_URL=http://127.0.0.1:11434

# Development models (smaller/faster for testing)
OLLAMA_TEXT_MODEL=llama3.2:latest
OLLAMA_VISION_MODEL=gemma3:latest
OLLAMA_EMBEDDING_MODEL=mxbai-embed-large
OLLAMA_MULTIMODAL_MODEL=gemma3:latest

# ChromaDB configuration (optional, defaults to local)
# CHROMA_SERVER_PORT=8000

# Debug logging (uncomment to enable verbose logging)
# DEBUG=stratosort:*

# Skip certain startup checks for faster iteration
# SKIP_OLLAMA_CHECK=true
